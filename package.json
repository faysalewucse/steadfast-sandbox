{
  "name": "steadfast-sandbox",
  "version": "1.0.0",
  "description": "Local mock server for Steadfast Courier Limited API v1",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "nodemon"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
